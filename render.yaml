services:
  - type: web
    name: sistema-manutencao-sakamoto
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python patch_login.py && gunicorn app:app
    healthCheckPath: /login
    envVars:
      - key: SECRET_KEY
        generateValue: true
